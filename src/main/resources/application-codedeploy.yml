blue-badge:
  auth-server:
    url: https://authorisation-service.${bb_env}.${url}/bb_auth
    client-id: bb_user_management_id
     ***REMOVED***
  notify:
    secretEnv: ${bb_env}
     ***REMOVED***
    templates:
      NEW_USER: a364e3cb-a72b-41fa-b28f-e7317525d028
      RESET_PASSWORD: 2ee75a54-8adc-41aa-ac06-5dfd724f48cb
      PASSWORD_RESET_SUCCESS: ea020bfb-cde8-4605-8726-248a38cc6bf2
  reference-data-service:
    service-host:
      host: reference-service.${bb_env}.${url}
      port: 443
      scheme: https

management:
  server:
    port: ${bb_health:-8081}
    servlet:
      context-path: /manage
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  main:
    banner-mode: "off"
  datasource:
     ***REMOVED***
    username: ${bb_db_username:-developer}
    url: jdbc:postgresql://${RDS_HOSTNAME}:5432/${bb_database:-bb_dev}?currentSchema=message&sslmode=verify-full

server:
  port: ${bb_port:-8080}
  servlet:
    context-path: /uk-gov-dft/message-service/1.0.0

